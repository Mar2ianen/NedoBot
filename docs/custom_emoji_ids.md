# Custom Emoji IDs

Бот читает premium/custom emoji из входящих сообщений как `custom_emoji_id`.

Текущий emoji для комментариев:

```env
COMMENT_CUSTOM_EMOJI_ID=5445092965875729965
```

Полная выгрузка из последнего сообщения лежит в `docs/custom_emoji_ids.tsv`.
